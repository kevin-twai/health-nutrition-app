# 部署測試環境變數
NODE_ENV=production

# 資料庫配置
POSTGRES_DB=health_tracker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=test_password_123

MONGODB_USER=admin
MONGODB_PASSWORD=test_mongo_123
MONGODB_DB=health_tracker_nutrition

REDIS_PASSWORD=test_redis_123

# JWT 配置
JWT_SECRET=test_jwt_secret_for_deployment_testing_only

# API 金鑰 (測試用)
OPENAI_API_KEY=test_openai_key
GOOGLE_VISION_API_KEY=test_vision_key

# AWS 配置 (測試用)
AWS_ACCESS_KEY_ID=test_aws_key
AWS_SECRET_ACCESS_KEY=test_aws_secret
AWS_REGION=us-west-2

# 監控配置
GRAFANA_ADMIN_PASSWORD=admin123

# 備份配置
S3_BACKUP_BUCKET=health-nutrition-backups-test

# Web 應用配置
NEXT_PUBLIC_API_URL=http://localhost
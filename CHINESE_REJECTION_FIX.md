# 🔧 修復：支援中文拒絕訊息檢測

## 📋 問題描述

### 症狀
用戶上傳圖片後，系統顯示「無法識別的食物」，但實際上 OpenAI API 已經返回了回應。

### 根本原因
從 Render 日誌中發現：

```
✅ ChatGPT Vision API 內容: 抱歉，我無法識別這張圖片中的人物或物品。不過，我可以幫助你分析食物的營養成分或其他問題。
```

**問題分析：**
1. OpenAI 檢測到圖片中有「人物」，觸發了內容政策
2. OpenAI 返回了**中文**的拒絕訊息：「抱歉，我無法識別...」
3. 原有代碼只檢測**英文**拒絕訊息（"I'm sorry", "I can't" 等）
4. 中文拒絕訊息沒有被檢測到，導致：
   - ❌ 沒有觸發重試機制
   - ❌ 沒有使用更強的 prompt 重試
   - ❌ 直接進入 JSON 解析，失敗後使用回退數據

## ✅ 解決方案

### 修改內容

在 `apps/api/src/simple-server.js` 中，更新了三處拒絕訊息檢測邏輯：

#### 1. 主要檢測邏輯（第 623 行）

**修改前：**
```javascript
if (content.includes("I'm sorry") || content.includes("I can't assist") || content.includes("I cannot") || content.includes("I can't help")) {
  // 處理拒絕...
}
```

**修改後：**
```javascript
const isRejected = content.includes("I'm sorry") || 
                   content.includes("I can't assist") || 
                   content.includes("I cannot") || 
                   content.includes("I can't help") ||
                   content.includes("抱歉") ||
                   content.includes("無法識別") ||
                   content.includes("无法识别") ||
                   content.includes("不能") ||
                   content.includes("無法分析");

if (isRejected) {
  // 處理拒絕...
}
```

#### 2. 重試後的檢測邏輯（第 203 行）

同樣添加了中文拒絕訊息的檢測。

#### 3. JSON 解析失敗後的檢測（第 658 行）

同樣添加了中文拒絕訊息的檢測。

### 支援的拒絕訊息

現在系統可以檢測以下拒絕訊息：

**英文：**
- "I'm sorry"
- "I can't assist"
- "I cannot"
- "I can't help"

**中文（繁體）：**
- "抱歉"
- "無法識別"
- "無法分析"
- "不能"

**中文（簡體）：**
- "无法识别"

## 🎯 預期效果

修復後，當 OpenAI 返回中文拒絕訊息時：

1. ✅ **正確檢測拒絕** - 系統會識別中文拒絕訊息
2. ✅ **自動重試** - 觸發重試機制（最多 2 次）
3. ✅ **使用更強 prompt** - 使用專門設計的 prompt 重試
4. ✅ **更好的日誌** - 記錄拒絕原因和重試過程

## 📊 測試建議

### 測試場景 1：有人物的圖片
上傳包含人物或手部的食物圖片，觀察：
- 系統是否檢測到拒絕
- 是否自動重試
- 重試後是否成功

### 測試場景 2：純食物圖片
上傳純食物圖片（無人物、手部），確認：
- 正常識別功能不受影響
- 識別結果準確

### 查看日誌
在 Render Dashboard 查看日誌，應該看到：

**成功檢測拒絕：**
```
❌ OpenAI 拒絕分析此圖片
   拒絕原因: 抱歉，我無法識別這張圖片中的人物或物品...
   當前重試次數: 0
🔄 嘗試重試 (1/2)，使用更明確的 prompt...
```

**重試成功：**
```
✅ ChatGPT Vision API 重試成功！
✅ 重試版本 JSON 解析成功
```

**重試仍失敗：**
```
❌ 重試後仍被拒絕
```

## 🚀 部署狀態

- ✅ 代碼已提交到 GitHub
- ✅ Render 自動部署中
- ⏳ 等待部署完成（約 2-3 分鐘）

## 📝 後續建議

### 短期建議
1. **測試修復效果** - 使用相同的圖片重新測試
2. **收集更多案例** - 記錄哪些圖片會被拒絕
3. **優化拍攝指南** - 提供用戶拍攝建議

### 長期建議
1. **圖片預處理** - 在發送前自動裁剪或模糊化人物
2. **多模型支援** - 添加備用的圖片識別 API
3. **用戶提示** - 當檢測到拒絕時，給用戶明確的提示

## 🔗 相關文件

- `apps/api/src/simple-server.js` - 主要修改文件
- `OPENAI_REJECTION_FIX.md` - 之前的修復文檔
- `DEBUG_OPENAI_VISION.md` - 調試指南

## 📞 需要幫助？

如果問題仍然存在：
1. 查看 Render 日誌中的詳細錯誤訊息
2. 確認圖片內容（是否有人物、手部等）
3. 嘗試重新拍攝圖片（避免人物入鏡）
4. 分享日誌內容以獲得進一步協助

---

**修復時間：** 2025-01-12
**版本：** 1.0.4
**狀態：** ✅ 已部署
